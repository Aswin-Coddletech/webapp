import {
  onSearchFilterChange,
  onPagenatationChange
} from "src/redux/utils/actions";

import { getSellerList } from "src/redux/seller/actions";

export default {
  getSellerList,
  onSearchFilterChange,
  onPagenatationChange
};
